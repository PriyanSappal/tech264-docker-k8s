---
apiVersion: v1
kind: Service
metadata:
  name: mongo-service
  namespace: default
spec:
  selector:
    app: mongo
  ports:
    - nodePort: 30002
      port: 27017
      targetPort: 27017
  type: NodePort